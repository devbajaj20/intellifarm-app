import 'package:flutter/material.dart';

class YieldPredictionScreen extends StatelessWidget {
  const YieldPredictionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Yield Prediction Screen')),
      body: const Center(child: Text('Yield Prediction Screen Module')),
    );
  }
}
